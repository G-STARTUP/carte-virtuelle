RewriteEngine On

# API rewrites (placer AVANT la règle SPA globale)
RewriteRule ^api/admin/?$ api/admin.php [QSA,L]
RewriteRule ^api/user/?$ api/user.php [QSA,L]
RewriteRule ^api/wallets/(.*)$ api/wallets.php?path=$1 [QSA,L]
RewriteRule ^api/wallets/?$ api/wallets.php [QSA,L]
RewriteRule ^api/cards/(.*)$ api/cards.php?path=$1 [QSA,L]
RewriteRule ^api/customer/?$ api/customer.php [QSA,L]
RewriteRule ^api/fund/?$ api/fund.php [QSA,L]
RewriteRule ^api/webhook/?$ api/webhook.php [QSA,L]
RewriteRule ^api/auth/(register|login)$ api/auth.php?action=$1 [QSA,L]
RewriteRule ^api/install/?$ api/install.php [QSA,L]
# Alias /install.php si appelé directement (évite 404 quand utilisateur tape racine)
RewriteRule ^install\.php$ api/install.php [QSA,L]

# SPA fallback (toutes les autres routes front)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [QSA,L]

# Sécurité basique (adapter selon besoin)
Header set X-Frame-Options "SAMEORIGIN"
Header set X-Content-Type-Options "nosniff"
Header always set Referrer-Policy "strict-origin-when-cross-origin"
Header set X-XSS-Protection "1; mode=block"
