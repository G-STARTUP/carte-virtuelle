; Example environment configuration (DO NOT COMMIT real secrets)
; Copy this file to secure/env.ini (create the secure/ directory) and replace values.
; Ensure secure/env.ini is NOT publicly accessible and is ignored by git.

; --- MySQL database credentials ---
MYSQL_HOST=localhost
MYSQL_DATABASE=u540259652_gwapcarte
MYSQL_USER=u540259652_gwapcarte
MYSQL_PASSWORD=CHANGE_ME

; --- JWT signing secret (long random string) ---
JWT_SECRET=CHANGE_THIS_JWT_SECRET

; --- Install protection token (use once then delete install.php) ---
INSTALL_SECRET=CHANGE_INSTALL_SECRET

; --- Strowallet API integration ---
STROWALLET_BASE_URL=https://api.strowallet.com/
STROWALLET_API_KEY=CHANGE_STROWALLET_KEY

; Optionally tune rate limiting (defaults in code if omitted)
; RATE_LIMIT_MAX=60
; RATE_LIMIT_WINDOW=60

; After setting real values on the server:
; 1. Run /install.php?token=INSTALL_SECRET once.
; 2. Delete api/install.php.
; 3. Test /api/auth.php register/login.
