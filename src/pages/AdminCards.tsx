import React from "react";

const AdminCards: React.FC = () => {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold">Administration - Cartes</h1>
      <p className="text-muted-foreground text-sm max-w-prose">
        Liste de gestion des cartes (placeholder). Prévoir filtrage par statut, devise, utilisateur.
      </p>
    </div>
  );
};

export default AdminCards;
